<%= form_with(model: shortcut_entry) do |form| %>
  <% if shortcut_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shortcut_entry.errors.count, "error") %> prohibited this shortcut_entry from being saved:</h2>

      <ul>
        <% shortcut_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :shortcut_key_id, style: "display: block" %>
    <%= form.text_field :shortcut_key_id %>
  </div>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.text_field :account_id %>
  </div>

  <div>
    <%= form.label :entry_type, style: "display: block" %>
    <%= form.number_field :entry_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
